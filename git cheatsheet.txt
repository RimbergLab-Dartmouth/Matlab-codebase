*** stage all files (new, deleted, modified)
git add -A


*** reset all modifications and deletion and clean all untracked files
git reset --hard
git clean -f -d


*** Check if a file is tracked, returns error if not tracked
git ls-files --error-unmatch <file name>

*** Reset to previous commit
git log --oneline     and find the disired HEAD
git reset 94515b9 --hard
git push origin dev_Chris --hard
WARNING: the hard push is necessary because the HEAD of local is behind remote. 
This will remove all commit records after the recent reverted one 94515b9



*** Ultimate reset:
delete the directory and git clone + url again, this should work in theory but is untested